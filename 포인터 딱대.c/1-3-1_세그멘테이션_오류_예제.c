#include <stdio.h>

int main() {
    // "int a = 10;" 얘가 있어야 함
    int *p /* "= &a" */;       // 포인터 선언 (초기화 X)
    *p = 100;     // ❌ p는 아직 아무 주소도 가리키지 않음 → 세그멘테이션 오류 발생! 얘를 할려면

    printf("p가 가리키는 값: %d\n", *p); // 이 줄까지 도달하지 못할 수 있음
    return 0;
}
